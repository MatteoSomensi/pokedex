package com.example.pokedex.theme

import androidx.compose.ui.graphics.Color

/** Primary light tone used by the dark color scheme. */
val Purple80 = Color(0xFFD0BCFF)

/** Secondary light tone used by the dark color scheme. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Tertiary light tone used by the dark color scheme. */
val Pink80 = Color(0xFFEFB8C8)

/** Primary dark tone used by the light color scheme. */
val Purple40 = Color(0xFF6650a4)

/** Secondary dark tone used by the light color scheme. */
val PurpleGrey40 = Color(0xFF625b71)

/** Tertiary dark tone used by the light color scheme. */
val Pink40 = Color(0xFF7D5260)
